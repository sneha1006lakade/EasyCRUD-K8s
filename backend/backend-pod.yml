apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    app: backend
spec:
  containers:
    - name: backend
      image: 1006sneha/k8s_backend:latest
      ports:
        - containerPort: 8085
